# Despliegue del proyecto

El proyecto se desplegó utilizando Docker y Docker Compose.

## Puerto de despliegue

La aplicación se muestra en el puerto:

http://localhost:8088

## Comando para construir y levantar el contenedor

```bash
docker compose up -d --build